- 项目铁律：所有可视化产物（HTML报告、时间轴、卡片、浏览器等）必须复用 generate_html_report.py 的 _STYLE + ink_theme 宣纸水墨设计体系，不得自建独立CSS。这是项目定海神针，UI统一是最高优先级。
- 用户偏好：可视化产物（HTML浏览器等）对终端用户价值有限，用户更看重 agent 能检索医案并用通俗语言解释。后续优化方向应聚焦"agent 解释能力"而非静态展示。
- 优化方向铁律：终端用户用的是对话式AI agent，不是CLI/HTML。所有优化应聚焦"提升agent检索+解释能力"，而非面向开发者的可视化产物/CLI工具。P2级可视化类OPT优先级应低于agent能力类。
- 铁律：每次推送前必须在本地跑完仓库同款CI全部测试（22项），全绿后才允许git push。不得跳过。CI测试清单见 .github/workflows/ci.yml。
